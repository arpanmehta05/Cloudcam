// Backward-compatible re-export — original consumers can still import from this path
// Canonical location is now: backend/src/modules/azure/controllers/live-action.controller.ts
export { azureLiveActionPost } from "../../modules/azure/controllers/live-action.controller";
